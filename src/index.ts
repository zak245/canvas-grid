/**
 * Grid Engine Library - Main Entry Point
 * 
 * An opinionated, batteries-included grid engine with:
 * - Canvas rendering (high performance)
 * - Built-in cell types (text, number, date, boolean, select, etc.)
 * - Full interaction handling (mouse, keyboard, touch)
 * - Event-driven architecture
 * - React integration (optional)
 */

// Core exports
export * from './core';

// React integration (re-export for convenience)
export * from './react';
